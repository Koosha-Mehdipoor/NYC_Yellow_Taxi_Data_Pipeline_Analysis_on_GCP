config {
    type: "table",
    database: "trip-record-data",
    schema: "serving_layer",
    name: "dim_payment",

}

SELECT
DISTINCT
  payment_type,
  paymenttype_name
FROM
  ${ref("V_STG_trip_data")}
